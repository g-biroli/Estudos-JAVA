				// aprendendo a manipular o java para que use a herencia com mais de um pai.
				// esse metodo se faz a partir da criacao de interface
				// a herencia so se pode herdar de 1 pai, porem se pode criar interfaces e assim implementar alem de 
				// uma classe, interfaces tbm

public interface motor {
		public static final String cilindrada = ""; //o fato de colocar = é inicializar alguma coisa em java
		public static final double precio = 0;		// nesse caso se inicialisa para que a interface de certo
													// apenas assim a interface funciona
		
		
}
